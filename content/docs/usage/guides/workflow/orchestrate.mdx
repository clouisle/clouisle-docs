---
title: 编排工作流
description: 从开始节点到输出节点构建可运行的类型化流程
icon: Workflow
---

工作流编辑器使用 React Flow 画布。左侧节点面板负责添加节点，中央画布负责连接流程，右侧抽屉负责配置选中节点。

## 创建与保存草稿

1. 前往**应用** > **创建应用** > **工作流**。
2. 在**工作流设置**填写名称、描述、可见性。
3. 选择开始节点：**用户输入**适合表单，**触发器**适合 API 或 Webhook 调用（Cron 定时触发尚未实现）。
4. 从节点面板拖拽节点到画布，或点击**添加节点**。
5. 拖动源句柄到目标句柄创建连线。开始节点不能删除。
6. 选择**保存**。编辑器会记录最近保存时间和未保存状态。

<figure>
  <img src="https://rustfs-endpoint.yhnotes.com/content/workflow-editor-canvas.png" alt="工作流编辑器画布" />
  <figcaption>工作流编辑器画布</figcaption>
</figure>

## 定义变量

开始节点使用工作流级变量。变量类型包括文本、段落、下拉选项、数字、布尔、数组、对象、文件、图片、多文件和多图片。每个变量可设置必填、默认值、标签和描述。

引用变量时使用编辑器选择器；不要手写不存在的名称。当前定义格式 `schema_version: 2` 支持原生对象和数组传递。

## 配置节点

按任务配置：

- **LLM**：选择模型、提示词、温度（默认 `0.7`，范围 `0-2`）、`max_tokens` 与 `top_p`。
- **Agent**：调用已发布 Agent 并传入消息模板。
- **知识库检索**：选择知识库、查询模板、Top K（默认 `5`，范围 `1-100`）与分数阈值（默认 `0.3`）。
- **条件分支**：配置表达式和 true/false 输出。
- **代码执行**：选择 Python/JavaScript，默认超时 `30` 秒，范围 `1-300` 秒。
- **HTTP 请求**：配置 GET/POST/PUT/PATCH/DELETE，默认超时 `30` 秒，范围 `1-300` 秒。
- **工具**：选择工具并映射参数。
- **变量赋值/聚合/模板**：把上游值整理为后续节点使用的结构。

## 发布前检查

选择**检查清单**，解决模型未选择、输入变量无来源、节点无输入、输出未定义、条件分支不完整、工具必填参数缺失、循环容器不完整等问题。校验通过后再调试运行和发布。

## 退出与丢失更改

返回应用列表或关闭页面前，如果存在未保存更改，编辑器会显示确认对话框。选择**不保存离开**会丢弃当前草稿更改。
